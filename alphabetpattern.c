/*alphabet patten that looks like spaced A*/
#include <stdio.h>
int main()
{
    int a,b,c;
    for(a=1;a<=7;a++)
    {
        for(b=1;b<=13;b++)
        {
            if()
            printf("%c",);
            else
            printf();
        }
        printf("\n");
    }
    return 0;
}