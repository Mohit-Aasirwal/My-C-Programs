#include <stdio.h>
int main()
{
    int x,y;
    printf("enter the coordinates of point\n");
    scanf("%d%d",&x,&y);

    if (x==0)
    printf("point lies on x axis\n");
    if (y==0)
    printf("point lies on y axis\n");
    if (x=0)
}
